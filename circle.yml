machine:
  services:
    - docker

deployment:
  hub:
    branch: master
    commands:
      - grunt build
      - docker build -t centurylink/lorry-ui:qa .
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - docker push centurylink/lorry-ui:qa
      - ssh root@$QA_SERVER_IP "./lorry_ui.sh"
  Production:
    branch: release
    commands:
      - grunt build
      - docker build -t centurylink/lorry-ui:latest .
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - docker push centurylink/lorry-ui:latest
      - ssh root@$PRO_SERVER_IP "./lorry_ui.sh"
